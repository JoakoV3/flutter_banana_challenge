import 'package:dio/dio.dart';
import 'package:flutter/material.dart';

/// url is here because it's a public api, if it wasn't it should be in env variables
const url = 'https://dummyjson.com';
final dio = Dio();
const primaryColor = Color(0xff9e007e);
